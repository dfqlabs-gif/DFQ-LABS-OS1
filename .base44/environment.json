{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key from https://aistudio.google.com/apikey — required for all AI/DM generation features.",
      "requiredAtBoot": false
    }
  ]
}
